# Agent-Tab – Browser-Check (localhost:5173)

Erzeugt mit `python scripts/ui_check/agent_check.py` bei laufender API + Vite. Konsolenfehler: **0** (0 = Pflicht). Bilder gitignored (Guardian S9).

## Screenshots

- **01_agent_tab.png** — Agent-Tab geladen (Konsole geprüft).
- **02_freigabe_mit_judge.png** — Freigabe erreicht, Beleg-Prüfung sichtbar (4 bestätigt).

## Auffälligkeiten

- Keine. Agent-Tab lädt fehlerfrei, Beleg-Prüfung und Freigabe intakt.
